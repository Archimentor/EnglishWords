# 영단어 5000 마스터 개발 현황과 후속 계획

기준일: 2026-07-11

이 문서는 실제로 완료된 앱 수직 슬라이스와 앞으로 검수해야 할 대량 콘텐츠를 구분합니다. **앱 기능 완료는 전체 콘텐츠 릴리스 완료를 뜻하지 않습니다.**

현재 상태는 **수직 슬라이스 엔지니어링 완료 / 전체 검수 콘텐츠 릴리스 미완료**입니다. 저장소에는 자동 배포·호스팅·CI 설정이 없으므로 배포 완료로 표현하지 않습니다.

## 1. 완료된 수직 슬라이스

| 영역 | 완료 내용 | 검증 근거 |
| --- | --- | --- |
| 기반 | React·TypeScript·Vite 프로젝트, JSON 스키마와 런타임 검증, 개발/릴리스 검증 분리 | `tests/data-contract.test.ts`, `npm run check` |
| 상태 | 버전 1 로컬 상태, 손상 복구, 저장 실패 경고, 메뉴·세션·숙련도·최근 7회 퀴즈 복원 | `src/state/*.test.ts*` |
| 탐색 | 7개 최상위 메뉴, 문맥별 하위 메뉴, 레벨 대시보드·소설·단어장, A1~C1 문법 | `src/app/App.test.tsx` |
| 참조 | 단어·구동사 통합 검색, 42개 문법 노드, 레벨별 소설과 커버리지 표시 | 각 feature 테스트 |
| 학습 | 최대 500개 고유 큐, 카드 플립, 의미·예문, 5단계 난이도, TTS, 레벨별 세션 | `StudyView.test.tsx`, `queue.test.ts` |
| 퀴즈 | 6종 문항, 즉시 채점, 수동 다음, 결과 통계, 오답 전용 복습 | quiz 단위·통합 테스트 |
| 연동 | 오답 +15%/+30%, 연속 오답 첫 3슬롯 우선 노출, 일반 세션 보존 | mastery·queue·앱 통합 테스트 |
| 품질 | 로딩·재시도·경합 방지, 키보드 접근성, 390px 반응형, 디자인 토큰 | 앱 테스트와 데스크톱·모바일 브라우저 QA |

수직 슬라이스의 대표 데이터는 단어 32개, 구동사 8개, 문법 노드 42개, 소설 4개입니다. 기능·경계 조건·상태 전이는 이 데이터로 끝까지 실행됩니다.

최종 인수 시점의 근거는 `npm run check` 통과(25개 테스트 파일, 255개 테스트), `git diff main...HEAD --check` 통과, 1280×900 및 390×844 브라우저 핵심 동선 통과입니다. 두 화면에서 콘솔 오류와 문서 전체 가로 넘침은 없었습니다.

## 2. 요구사항 추적표

| 요구사항 | 자동화된 근거 | 실제 화면 확인 |
| --- | --- | --- |
| 메뉴/컨텍스트 상태 전이 | `학습을 누르면 레벨 메뉴를 열고 컨텍스트 레벨 선택 즉시 학습으로 진입한다`, `단어장은 최상위 메뉴가 아니라 레벨 컨텍스트 메뉴다` | 최상위 메뉴와 레벨 하위 메뉴 전환 |
| 새로고침 복원 | `같은 저장소로 다시 렌더링하면 메뉴 선택을 복원한다`, `일반 학습 세션은 평가 후 새로고침해도 다음 위치를 복원한다` | 메뉴·학습 카드 변경 후 새로고침 |
| 단어+구동사 검색 | `단어와 구동사를 같은 검색 결과에서 형태·뜻까지 검색한다` | 단어장에서 `wake up` 검색 |
| 42개 문법 노드 탐색 | `문법 대시보드는 전체 42개와 레벨별 수량을 보여준다`, `전체 문법 순서로 이전·다음 이동하고 stale 노드는 안전하게 처리한다` | A1~C1 노드와 이전·다음 이동 |
| 최대 500개 비복원 학습 큐 | `저장 세션이 없으면 최대 500개 고유 큐를 한 번 생성해 즉시 저장한다`, `returns exactly 500 unique items from 600 candidates` | 학습 진행률과 새로고침 후 위치 |
| 카드 의미+예문 동시 노출 | `카드를 뒤집으면 모든 뜻과 예문을 보여주고 다음 카드에서 닫힌다` | 플래시카드 뒤집기 |
| 5단계 난이도 | `uses the documented 5 by 5 matrix`, `난이도 변경은 현재 카드를 유지하고 뒤쪽만 재가중하며 숙련도를 건드리지 않는다` | 다섯 난이도 버튼 |
| TTS 실패 대체 | `필수 브라우저 API가 없으면 발음 포트를 만들지 않는다`, `TTS 실패를 알리되 카드 학습은 계속할 수 있다` | 음성 미지원 상태의 비차단 안내 |
| 6종 퀴즈 | `여섯 유형의 전체 한국어 이름을 표시하고 선택 유형을 바꾼다`, `creates a %s question with an answer and source` | 여섯 유형 선택·문항 렌더링 |
| 즉시 채점+수동 다음 | `객관식은 즉시 피드백 후에도 다음문제 전까지 같은 문항을 유지한다` | 오답 제출 후 `다음문제` 클릭 |
| +15%/+30% 오답 보정 | `applies the documented single and streak mistake boosts`, `같은 퀴즈 오답이 누적되면 다음 학습 큐 첫 3슬롯으로 전파된다` | 반복 오답 후 학습 재진입 |
| 결과 점수·정답률·히트맵·오답 유형 | `summarizes score, accuracy, every type, heatmap, and unique wrong items`, `마지막 결과에서 세션을 한 번 저장하고 점수·히트맵·유형 통계를 표시한다` | 퀴즈 결과 화면 |
| 개발/릴리스 데이터 검증 분리 | `development validation and CLI succeed with concise counts`, `release validation reports only per-level word and phrasal count gaps` | 두 npm 검증 명령의 종료 코드와 출력 |

테스트 이름은 현재 코드의 실제 테스트명이며, 관련 파일은 `src/app`, `src/features`, `src/domain`, `src/state`, `tests` 아래에 있습니다.

## 3. 남은 검수 콘텐츠

| 콘텐츠 | 현재 | 릴리스 목표 | 남은 작업 |
| --- | ---: | ---: | --- |
| 단어 | 레벨별 8, 총 32 | 5,000 | 기초 500, 유치원 500, 초등학교 1,500, 중학교 2,500개로 확장하고 교차 레벨·워드패밀리 검수 |
| 구동사 | 레벨별 2, 총 8 | 1,000 | 레벨별 250개, 전체 목록과 레벨 참조 일치, 뜻·용례·예문 검수 |
| 문법 노드 | 42 | 42 | 노드 수는 충족. 각 노드의 장문 설명·산출 과제·오류 교정 콘텐츠를 편집 검수 |
| 소설 | 레벨별 1개, 총 4 | 레벨별 정확히 1개, 총 4 | 파일 수는 충족. 최종 wordlist의 모든 lemma를 1회 이상 포함하고, 기록한 품사·형태가 해당 word entry와 일치하도록 수동 개작·검수 |

로그인, 서버 데이터베이스, 여러 기기 동기화, 관리자 CMS는 현재 범위의 비목표입니다.

## 4. 후속 작업 순서

1. 레벨별 단어를 목표 수량까지 편집·검수하고 `validate:data`를 유지합니다.
2. 구동사 1,000개와 레벨별 250개 참조를 함께 구축합니다.
3. 42개 문법 노드의 장문 콘텐츠와 산출 과제를 완성합니다.
4. 확정된 레벨별 wordlist lemma를 기준으로 네 소설을 수동 개작하고 단어 100% 커버리지를 검수합니다.
5. 표본 리뷰, 중복·가족형·예문·난이도 편집 QA를 수행합니다.
6. `npm run validate:release`와 전체 품질 게이트를 통과한 빌드만 배포합니다.
7. 실제 정적 호스팅과 CI를 설정한 뒤 production URL에서 smoke test를 통과시킵니다.

대량 콘텐츠는 자동 생성 결과를 곧바로 완료 데이터로 간주하지 않습니다. 작은 배치로 추가하고 데이터 검증, 편집 검수, 브라우저 표본 확인을 반복합니다.

## 5. 품질 및 릴리스 게이트

### 앱 수직 슬라이스 게이트

```powershell
npm ci
npm run check
```

개발 데이터 검증, ESLint, 전체 테스트, TypeScript와 Vite 빌드가 모두 성공해야 합니다. 데스크톱과 390px 모바일에서 대시보드 → `wake up` 검색 → 카드 뒤집기·난이도 기록 → 퀴즈 오답 즉시 피드백 → 명시적 `다음문제` → 결과 → 우선 복습 → 새로고침 복원 흐름도 오류 없이 동작해야 합니다. 이때 콘솔 오류와 잘리거나 문서 전체를 넘치는 UI가 없어야 합니다.

### 전체 콘텐츠 릴리스 게이트

```powershell
npm run validate:release
npm run check
```

두 명령이 모두 exit code 0이어야 전체 콘텐츠 릴리스 준비가 완료됩니다. 현재 `validate:release`의 `WORD_COUNT_MISMATCH`, `PHRASAL_COUNT_MISMATCH` 실패는 공개된 잔여 작업이며 수직 슬라이스의 실패로 숨기거나 성공 처리하지 않습니다.

## 6. 변경 원칙

- `개발계획.md`, `개발(구조).md`, `개발(문법).md`를 제품 요구사항의 기준으로 유지합니다.
- 화면은 상태 전환만 요청하고, 큐·채점·숙련도 계산은 `src/domain`의 순수 함수로 둡니다.
- 브라우저에서 읽는 콘텐츠의 정규 경로는 `public/data`입니다.
- 행동을 바꿀 때는 실패하는 테스트를 먼저 추가하고 `npm run check`로 회귀를 막습니다.
- 개발 수량과 릴리스 수량을 UI·문서·검증 출력에서 항상 구분합니다.

## 7. 원문 목표와 채택 구현의 관계

초기 원문에는 대안 또는 미래 구조도 함께 기록되어 있습니다. 현재 채택 구현은 다음과 같이 확정했습니다.

- 예상 스택 중 React를 선택하고 TypeScript 6·Vite 8·plain CSS로 구현했습니다. Vue, Tailwind, Zustand, Recoil, Express는 사용하지 않습니다.
- 상태는 React Context와 reducer, 브라우저 `localStorage`로 관리합니다.
- 최상위 메뉴는 상세 상태 전이를 정의한 `개발(구조).md`에 따라 네 레벨과 `문법/학습/퀴즈`로 구성하며, `단어장`은 레벨 컨텍스트 메뉴입니다.
- 브라우저 정적 데이터 경로는 원문의 예시 `data/...`를 Vite 관례에 맞춰 `public/data/...`로 확정했습니다.
- 문법은 노드별 HTML 파일을 생성하지 않고 `public/data/grammar/nodes.json`을 단일 데이터 소스로 렌더링합니다.

원문 문서는 제품 목표와 의도를 보존하고, 이 문서와 승인된 수직 슬라이스 설계는 현재 실제 구현 상태를 설명합니다.
