# 영단어 5000 마스터 홈페이지 구조 설계서 (개발(구조))

## 0. 문서 목적
- 기준 문서: `개발계획.md`, `개발(문법).md`, `index.html`
- 목적: 현재 제작 중인 홈페이지의 전체 트리 구조(정보구조, 화면구조, 데이터구조, 상태구조)를 단일 문서로 확정
- 범위: `index.html` 기반 단일 페이지 UI + 추후 데이터 연결 구조

---

## 1. 전체 정보구조(IA) 트리

```text
영단어 5000 마스터 (index.html)
├─ 상단 브랜드/상태
│  ├─ 타이틀: 영단어 5000 마스터
│  └─ 배지: UI 버전/상태
│
├─ 최상위 메뉴(Primary)
│  ├─ 레벨 축: 기초 / 유치원 / 초등학교 / 중학교
│  └─ 기능 축: 문법 / 학습 / 퀴즈
│
├─ 컨텍스트 메뉴(Secondary)
│  ├─ 레벨 컨텍스트: 대시보드 / 소설 / 단어장
│  ├─ 문법 컨텍스트: 대시보드 / A1 / A2 / B1 / B2 / C1
│  └─ 세션 컨텍스트(학습·퀴즈): 기초 / 유치원 / 초등학교 / 중학교
│
├─ KPI 영역
│  ├─ 현재 화면
│  ├─ 단어/노드 범위
│  └─ 커버리지/규칙 지표
│
└─ 본문 2열 레이아웃
   ├─ 좌측 컬럼(핵심 작업)
   │  ├─ 대시보드 패널
   │  ├─ 단어장 패널
   │  ├─ 문법 메인 패널
   │  ├─ 학습 패널
   │  └─ 퀴즈 문제 패널
   └─ 우측 컬럼(보조/연결)
      ├─ 퀴즈 유형 선택 패널
      ├─ 문법 보조 패널
      ├─ 구동사 1000 커버리지 패널
      └─ 레벨 소설 패널
```

---

## 2. 메뉴 동작 구조

### 2.1 최상위 메뉴 규칙
- `기초/유치원/초등학교/중학교` 클릭:
  - `section = 대시보드`로 진입
  - 컨텍스트 메뉴는 `대시보드/소설/단어장` 노출
- `문법` 클릭:
  - `section = 문법`, `grammarSection = 대시보드`
  - 컨텍스트 메뉴는 `대시보드 / A1 / A2 / B1 / B2 / C1` 노출
- `문법` > `A1` 클릭:
  - `grammarSection = A1`
  - 우측 보조 패널에 `A1-G01 ~ A1-G08` 버튼 목록 표시
  - 노드 버튼을 클릭하면 `문법 본문`에 해당 노드 상세가 렌더링
  - 각 노드 버튼은 실제 페이지 연결 단위(`data/grammar/A1/A1-Gxx.html`)로 이동 가능
- `문법` > `A2` 클릭:
  - `grammarSection = A2`
  - 노드 버튼 목록(`A2-G01~`)이 표시되고 각 버튼이 노드 상세 연결
- `문법` > `B1/B2/C1` 역시 동일 패턴으로 동작
- `학습` 클릭:
  - `section = 학습`
  - 컨텍스트 메뉴는 레벨 선택(`기초~중학교`) 노출
- `퀴즈` 클릭:
  - `section = 퀴즈`
  - 컨텍스트 메뉴는 레벨 선택(`기초~중학교`) 노출

### 2.2 하이라이트(활성 색상) 규칙
- 레벨 버튼 활성: `대시보드/소설/단어장` 컨텍스트에서만 적용
- 기능 버튼 활성: `문법/학습/퀴즈` 컨텍스트에서만 적용
- 서브메뉴 클릭 시: 해당 서브메뉴만 활성 표시

### 2.3 새로고침 유지 규칙
- 저장 키: `localStorage.wordMasterMainMenuState`
- 저장 필드:
  - `level`, `section`, `grammarSection`, `studyDifficulty`, `quizType`
- 동작:
  - 진입 시 상태 복원(`restoreMenuState`)
  - 메뉴/난이도/유형 변경 시 즉시 저장(`saveMenuState`)

---

## 3. 화면(패널) 트리와 설명

## 3.1 좌측 컬럼 패널

### A. `data-panel="대시보드"` 학습 관리
- 진행률 바
- 완료/미완료/오답 태그
- 레벨별 학습 상태 요약

### B. `data-panel="단어장"` 단어 검색/조회
- 검색 입력 + 검색 버튼
- 테이블 컬럼: `Word / 품사 / 뜻 / 예문`
- 일반 단어 + 구동사(phrasal verb) 동시 조회

### C. `data-panel="문법"` 문법 메인
- 문법 대시보드 뷰(`grammarDashboardView`)
  - 전체 노드 수, 오늘 포인트, 약점
- 문법 트리 뷰(`grammarTreeView`)
  - 문법 레벨 칩(`A1~C1`)
  - 선택한 레벨의 노드 목록 버튼 목록 노출
  - 노드 버튼(`A1-G01` 형태) 클릭 시 본문에 상세 반영
  - 노드 목록은 “클릭 메뉴”로 동작: 선택/활성 표시, 단일 선택만 본문 반영

### D. `data-panel="학습"` 플래시카드 학습
- 난이도 5단계:
  - `아주쉬움/쉬움/보통/어려움/아주어려움`
- 카드 구성:
  - 단어, 발음, 의미
  - 카드 뒤집기 시 의미+예문 동시 노출
- 세션 원칙:
  - 레벨별 500개 무작위 노출
  - 난이도/퀴즈 결과 기반 노출 빈도 보정

### E. `data-panel="퀴즈"` 퀴즈 문제
- 문제 카드(좌측 핵심 영역)
- 즉시 정답 공개
- 남은 문항 표시
- 문제 진행 피드백 표시

## 3.2 우측 컬럼 패널

### F. `data-panel="퀴즈"` 문제 유형 선택 카드(분리형)
- 6유형 버튼을 별도 카드로 분리
- 유형:
  - `4지선다 영어→한글`
  - `4지선다 한글→영어`
  - `문장 밑줄 단어 의미 선택`
  - `문장 빈칸 단어 선택`
  - `받아쓰기(듣기 입력)`
  - `문장 변환`

### G. `data-panel="문법"` 문법 보조 카드
- 문법 대시보드 상태 요약
- 상세 선택 시 노드별 보완 정보 표시

### H. `data-panel="소설"` 구동사 1000 관리
- 레벨별 분배 상태(기초/유치원/초등/중등)
- 목표: 총 1000개, 레벨 커버리지 100%

### I. `data-panel="소설"` 레벨 소설
- 레벨별 소설 1편
- 조건:
  - 수동 작성
  - 해당 레벨 단어 커버리지 100%
  - 하위 레벨은 상위 레벨 단어 사용 금지

---

## 4. 문법 트리 구조 (A1~C1)

`개발(문법).md` 기준 최종 트리:

```text
Grammar Root
├─ A1
│  ├─ A1-G01 문장뼈대
│  ├─ A1-G02 be동사/일반동사 현재
│  ├─ A1-G03 there is/are
│  ├─ A1-G04 현재진행/단순과거 기초
│  ├─ A1-G05 대명사
│  ├─ A1-G06 관사/지시사/전치사
│  ├─ A1-G07 can/can't + 명령문
│  └─ A1-G08 and/but/because + 빈도부사
├─ A2 Expansion
│  ├─ A2-G01 미래(will/be going to)
│  ├─ A2-G02 현재완료(since/for/just/already/yet)
│  ├─ A2-G03 비교급/최상급
│  ├─ A2-G04 수량표현/결정사
│  ├─ A2-G05 조동사 확장
│  ├─ A2-G06 수동태(현재/과거)
│  ├─ A2-G07 관계절 입문
│  ├─ A2-G08 재귀대명사
│  └─ A2-G09 to부정사/동명사 + 부가의문
├─ B1 Integration
│  ├─ B1-G01 시제 통합
│  ├─ B1-G02 조건문 0/1/2 + wish
│  ├─ B1-G03 간접화법
│  ├─ B1-G04 명사절/간접의문
│  ├─ B1-G05 관계절 확장
│  ├─ B1-G06 조동사 추측 + 수동 확장
│  ├─ B1-G07 used to/would
│  ├─ B1-G08 연결어 논리전개
│  └─ B1-G09 동명사/부정사 의미차
├─ B2 Complex Control
│  ├─ B2-G01 완료진행/미래완료
│  ├─ B2-G02 가정법 3형/혼합
│  ├─ B2-G03 modal perfect
│  ├─ B2-G04 분사절/축약관계절
│  ├─ B2-G05 강조/도치/분열문
│  ├─ B2-G06 사역/지각/수동 패턴
│  ├─ B2-G07 전치사+관계대명사
│  ├─ B2-G08 명사화/학술문체 기초
│  └─ B2-G09 복합 한정어
└─ C1 Precision & Register
   ├─ C1-G01 담화문법
   ├─ C1-G02 고급 도치/전위
   ├─ C1-G03 hedging/stance
   ├─ C1-G04 문장 밀도 제어
   ├─ C1-G05 레지스터 전환
   ├─ C1-G06 오류 최소화
   └─ C1-G07 직역 교정
```

### 4.1 노드 클릭 동작 규칙
- A1, A2, B1, B2, C1은 상위 노드 메뉴 버튼으로 동작한다.
- 상위 노드 클릭 시 하위 노드 목록이 화면 오른쪽/패널에서 버튼 형식으로 열린다.
- 하위 노드 버튼(`A1-G01`, `A2-G01` 등) 클릭 시 단일 노드 상세 페이지로 연결되어 본문/카드에 즉시 반영된다.

문법 페이지 운영 원칙:
- `A1-G01` 같은 노드 단위가 학습 단위
- 노드별로 상세 설명(규칙/예문/오류/과제)을 제공
- 문법은 데이터 목록(JSON)이 아니라, 각 노드 페이지를 HTML로 분리해 설명형 UI로 운영

---

## 5. 콘텐츠/데이터 트리 (목표 구조)

```text
data/
├─ content/
│  ├─ wordlists/
│  │  ├─ 기초.json
│  │  ├─ 유치원.json
│  │  ├─ 초등학교.json
│  │  └─ 중학교.json
│  ├─ phrasal-verbs/
│  │  ├─ top-1000.json
│  │  └─ by-level/
│  │     ├─ 기초.json
│  │     ├─ 유치원.json
│  │     ├─ 초등학교.json
│  │     └─ 중학교.json
│  ├─ stories/
│  │  ├─ 기초.json
│  │  ├─ 유치원.json
│  │  ├─ 초등학교.json
│  │  └─ 중학교.json
│  ├─ grammar/
│  │  ├─ dashboard.html
│  │  ├─ A1/
│  │  │  ├─ index.html
│  │  │  ├─ A1-G01.html
│  │  │  ├─ ...
│  │  │  └─ A1-G08.html
│  │  ├─ A2/
│  │  │  ├─ index.html
│  │  │  ├─ A2-G01.html
│  │  │  ├─ ...
│  │  │  └─ A2-G09.html
│  │  ├─ B1/
│  │  │  ├─ index.html
│  │  │  ├─ B1-G01.html
│  │  │  ├─ ...
│  │  │  └─ B1-G09.html
│  │  ├─ B2/
│  │  │  ├─ index.html
│  │  │  ├─ B2-G01.html
│  │  │  ├─ ...
│  │  │  └─ B2-G09.html
│  │  └─ C1/
│  │     ├─ index.html
│  │     ├─ C1-G01.html
│  │     └─ ...
│  ├─ schema/
│  │  ├─ wordlist.schema.json
│  │  ├─ phrasal.schema.json
│  │  ├─ story.schema.json
│  │  ├─ grammar-node.schema.json
│  │  ├─ learner-state.schema.json
│  │  └─ tracker.schema.json
├─ tracking/
│  ├─ learners/
│  │  ├─ profile.json
│  │  ├─ learners.json
│  │  ├─ progress/
│  │  │  ├─ learner-summary.json
│  │  │  ├─ level-progress.json
│  │  │  ├─ word-mastery.json
│  │  │  ├─ phrasal-mastery.json
│  │  │  ├─ grammar-mastery.json
│  │  │  └─ session-history.json
│  │  ├─ quiz/
│  │  │  ├─ responses.jsonl
│  │  │  ├─ mistake-bank.json
│  │  │  ├─ type-stats.json
│  │  │  └─ difficulty-stats.json
│  │  ├─ scheduler/
│  │  │  ├─ study-schedule.json
│  │  │  ├─ exposure-weight.json
│  │  │  ├─ priority-queue.json
│  │  │  └─ spacing-policy.json
│  │  └─ errors/
│  │     ├─ event-log.jsonl
│  │     └─ validation-log.jsonl
│  ├─ engine/
│  │  ├─ difficulty-rules.json
│  │  ├─ reprioritize-rules.json
│  │  ├─ error-weights.json
│  │  ├─ spacing-rules.json
│  │  └─ calibration.json
│  └─ exports/
│     ├─ daily.json
│     └─ monthly.json
└─ DEVELOPMENT/
   ├─ migration-history.json
   └─ release-notes.json
```

### 5.1 정교화된 데이터 책임

#### 5.1.1 content/wordlists
- 목적: 레벨별 단어 풀의 단일 진실 소스(Single Source)
- 핵심 스키마: `lemma`, `familyId`, `isFamilyHead`, `level`, `partOfSpeech`, `forms`, `meanings`, `ipa`, `examples`, `tags`
- 운영 규칙: 레벨별 중복 금지, 워드패밀리 head 하나로 정규화
- 산출: 학습/퀴즈 큐 빌드 입력

#### 5.1.2 content/phrasal-verbs
- top-1000은 전체 정합성 기준 집합
- by-level은 레벨별 커버리지와 목표 달성률을 저장
- 구문 단위(`baseVerb`, `particle`, `phrasalVerb`)와 의미 체계(`meaningKo`)를 묶음으로 관리
- 누락 시 `tracker/error`로 즉시 보고

#### 5.1.3 content/grammar
- dashboard.html: 전체 노드/완료율/오류군집 요약
- 각 레벨 index.html: 노드 목록과 선행노드, 난이도, 가중치 정보를 담은 네비게이션용 메타
- 각 노드 페이지(`Ax-Gyy.html`): 규칙, 예문, 오답 포인트, 산출 과제, 통과 기준을 담은 완전한 설명형 HTML 문서

#### 5.1.4 content/stories
- 레벨당 1편 수동 본문
- 필수 메타: `coverage.mustCoverAll`, `allowUpperLevelWords`, `usedWords`, `coverageRate`
- 소설 완료조건은 `학습자 노출 진입 조건`과 독립적으로 유지

#### 5.1.5 tracking/learners/progress
- learner-summary: 레벨, 일별 연속 학습률, 전체 정답률, 미완료 큐 크기
- level-progress: 레벨별 세션 수, 난이도별 노출 비율, 목표 대비 진도
- word-mastery: 단어별 `attempts`, `correct`, `wrong`, `streak`, `ease`, `lastSeenAt`, `nextDueAt`, `weight`, `lastLevel`
- phrasal-mastery: 구동사별 `attempts`, `wrong`, `lastSeenAt`, `priority`
- grammar-mastery: 노드별 마스터리(`accuracy`, `retryCount`, `mustReview`), 선행 조건 충족 여부
- session-history: 세션 시작-종료, 소요시간, 유형별 성과, 감점/보정 적용 내역

#### 5.1.6 tracking/learners/quiz
- responses: 응답 로그(`questionId`, `questionType`, `level`, `isCorrect`, `answerTimeMs`, `quizType`, `difficultyUsed`)
- mistake-bank: 오답 단어/구문 풀(`wrongCount`, `penaltyWeight`, `nextBoost`, `cooldownAt`, `linkedLevel`)
- type-stats: 6개 유형 정답률·반응시간·난이도 보정 반영치
- difficulty-stats: 난이도 버튼 선택 빈도, 선택-정답 편차, 오답 재출제 전파량

#### 5.1.7 tracking/scheduler
- study-schedule: 세션 큐 생성 이력, 난이도 조합, 시작/중단/복구 포인트
- exposure-weight: 난이도 기본 가중치 + 오답 가중치 + 규칙 가중치의 합산 값
- priority-queue: 학습/퀴즈 공통 후보 순서 저장(단어 ID, 우선순위, 삽입 시점)
- spacing-policy: 반복 노출 최소 간격 및 예외 정책(시험 직전 밀도 상승 등)

#### 5.1.8 engine 룰셋
- difficulty-rules: `아주쉬움~아주어려움` 빈도 정책
- reprioritize-rules: 오답 누적과 최근성 기반 재노출 정책
- error-weights: 1회 +15%, 2회 +30% 반영 규칙
- spacing-rules: 최소 노출 간격, 연속 정답 시 가중치 하향 정책
- calibration: 문법-학습-퀴즈 상호 피드백 융합 규칙

### 5.2 학습/퀴즈/문법 처리 구조(데이터 흐름)

```text
학습(세션 시작)
  ├─ level wordlists + phrasal by-level 로드
  ├─ word-mastery + phrasal-mastery 결합
  ├─ difficulty-rules 적용
  ├─ mistake-bank와 priority-queue 병합
  ├─ exposure-weight로 500개 샘플링
  ├─ 세션 큐 생성(learning queue)
  ├─ 플래시카드 응답 시 word-mastery 즉시 갱신
  └─ session-history 저장 + level-progress 반영

퀴즈(문항 생성)
  ├─ level + quizType으로 문항 풀 로드
  ├─ type-stats로 난이도별 기본 분포 계산
  ├─ mistake-bank에서 재노출 후보 상향 반영
  ├─ word/grammar mastery 연동 재가중치 적용
  ├─ 세션 응답별 responses 기록
  ├─ 오답은 mistake-bank 업서트
  ├─ 오답 단어 가중치 즉시 상향
  └─ quiz-stats와 learner-summary 반영

문법(노드 학습)
  ├─ grammar dashboard에서 노드 메타 조회
  ├─ grammar-mastery 선행요건 검사
  ├─ 미달성 노드면 재진단 큐로 라우팅
  ├─ 노드 과제 결과 저장
  └─ completion/오류코드 기반 추천 레벨/노드 계산
```

### 5.3 운영 지표
- 학습 지표: 회차별 정답률, 오답 간격, 레벨별 누적 숙련도
- 퀴즈 지표: 유형별 정확도, 반응속도, 재출제 효율, 연속 오답율
- 문법 지표: 노드 완료율, 선행노드 통과율, 재진단 반복 횟수
- 운영 지표: 큐 건강도(`mistakeBankRatio`, `prioritySaturation`, `overdueItems`)

### 5.4 필수 규칙(통합)
- 레벨 간 단어 중복 금지
- 워드패밀리 `isFamilyHead=true`는 각 패밀리 당 1개만 유지
- 단어 항목은 품사·동사변형·의미(빈도 상위 3~4개)·IPA·예문 2개 이상
- 구동사 1000개는 레벨별 100% 커버리지, 누락은 빌드 실패로 차단
- 학습/퀴즈 오답 단어는 최소 한 번 이상의 재노출 보정 적용
- schema 버전(`schemaVersion`)을 유지하고 마이그레이션 로그 남김

---

## 6. 프론트 상태(State) 구조

`index.html` 상태 객체 기준:

```text
state
├─ level: 현재 레벨
├─ section: 현재 메인 섹션(대시보드/소설/단어장/문법/학습/퀴즈)
├─ grammarSection: 문법 서브섹션(대시보드/A1~C1)
├─ studyDifficulty: 학습 난이도 5단계
├─ quizType: 퀴즈 유형 6종
├─ quizBlocked: 현재 문항 잠금 여부
├─ quizProgress: 현재 문항 번호
├─ quizTotal: 총 문항 수
└─ quizTimer: 자동 전환 타이머 핸들
```

상태 전이 핵심:
- 메뉴/서브메뉴/난이도/유형 선택 -> 즉시 `render()`
- `render()`에서 패널 표시/숨김, KPI, 메뉴 활성 상태, 퀴즈 화면 갱신

---

## 7. 사용자 플로우 트리

```text
공통 진입
├─ 레벨 학습관리 플로우
│  └─ 레벨 선택 -> (대시보드/소설/단어장)
├─ 문법 플로우
│  └─ 문법 -> (대시보드/A1~C1) -> 노드 상세
├─ 학습 플로우
│  └─ 학습 -> 레벨 선택 -> 플래시카드(500 랜덤) -> 난이도 기록
└─ 퀴즈 플로우
   └─ 퀴즈 -> 레벨 선택 -> 문제 유형 선택(우측 카드) -> 문제풀이(좌측 카드)
```

퀴즈-학습 연동 흐름:
- 퀴즈 오답 단어는 학습 큐 가중치 상향
- 학습 난이도 선택과 실제 정답률을 함께 반영해 노출 빈도 재계산

---

## 8. 구현 단계별 구조 우선순위

1. 데이터 구조 확정
- 레벨 단어/구동사/문법노드/소설 파일 체계 확정

2. 라우팅/상태 구조 고정
- 현재 단일 페이지 상태 전이 로직 확정
- 저장 상태(localStorage)와 UI 활성 규칙 확정

3. 콘텐츠 연결
- 단어장/학습/퀴즈/문법/소설에 실제 데이터 주입

4. 진도·오답 엔진 연결
- 학습 빈도 가중치, 퀴즈 오답 보정, 재노출 로직 연결

5. 고도화
- 문법 노드 상세 HTML 콘텐츠 정밀화
- 리포트/분석 뷰 추가


## 9. 폴더와 파일 트리 설계 (개발(구조))

```text
.
├─ data/
│  ├─ wordlists/
│  │  ├─ 기초.json
│  │  ├─ 유치원.json
│  │  ├─ 초등학교.json
│  │  └─ 중학교.json
│  ├─ phrasal-verbs/
│  │  ├─ top-1000.json
│  │  └─ by-level/
│  │     ├─ 기초.json
│  │     ├─ 유치원.json
│  │     ├─ 초등학교.json
│  │     └─ 중학교.json
│  ├─ stories/
│  │  ├─ 기초.json
│  │  ├─ 유치원.json
│  │  ├─ 초등학교.json
│  │  └─ 중학교.json
│  ├─ grammar/
│  │  ├─ dashboard.html
│  │  ├─ A1/
│  │  │  ├─ index.html
│  │  │  ├─ A1-G01.html
│  │  │  ├─ A1-G02.html
│  │  │  └─ ...
│  │  ├─ A2/
│  │  │  ├─ index.html
│  │  │  ├─ A2-G01.html
│  │  │  ├─ A2-G02.html
│  │  │  └─ ...
│  │  ├─ B1/
│  │  │  ├─ index.html
│  │  │  ├─ B1-G01.html
│  │  │  ├─ B1-G02.html
│  │  │  └─ ...
│  │  ├─ B2/
│  │  │  ├─ index.html
│  │  │  ├─ B2-G01.html
│  │  │  ├─ B2-G02.html
│  │  │  └─ ...
│  │  └─ C1/
│  │     ├─ index.html
│  │     ├─ C1-G01.html
│  │     ├─ C1-G02.html
│  │     └─ ...
│  └─ schema/ (선택)
│     ├─ wordlist.schema.json
│     ├─ phrasal.schema.json
│     ├─ story.schema.json
│     └─ grammar-node.schema.json
├─ index.html
├─ 개발계획.md
├─ 개발(문법).md
├─ 개발(구조).md
└─ DEVELOPMENT_PLAN.md
```

### 파일 목적 요약
- `wordlists/*.json`: 레벨별 단어원본 목록(단어, 품사, 품사별 의미·예문·발음)
- `phrasal-verbs/top-1000.json`: 구동사 총합 1000개 보관소
- `phrasal-verbs/by-level/*.json`: 레벨별 구동사 250개 분배표
- `stories/*.json`: 레벨별 소설 1개(수동 작성, 레벨 커버리지 규칙 적용)
- `grammar/dashboard.html`: 문법 전체 통합 대시보드 상태
- `grammar/레벨/index.html`: 레벨 내 노드 목록
- `grammar/레벨/Ax-Gyy.html`: 노드 단위 문법 상세 HTML 페이지
- `schema/*.json`: 추후 유효성 검사/자동화 연동용(선택)

