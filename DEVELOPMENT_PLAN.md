# 영단어 5000 마스터 개발 현황과 릴리스 잔여 게이트

기준일: 2026-08-20

이 문서는 저장소에 구현된 앱·콘텐츠 파이프라인·자동 검증과 실제 배포 확인을 구분합니다. **로컬 릴리스 게이트 완료는 production 배포 완료를 뜻하지 않습니다.**

현재 상태는 **최신 소스·승인 콘텐츠 재생성, 로컬 릴리스 게이트, 실제 브라우저 검증 완료 / production 게시 직전**입니다. 공개 저장소 `Archimentor/EnglishWords`를 이 단일 작업 경로의 `origin`으로 연결했고 GitHub Pages를 Actions 방식으로 활성화했습니다. 아직 이번 변경을 stage·commit·push하지 않았으므로 원격 Actions 성공과 production URL 동작은 아래 후속 게이트로 남아 있습니다.

## 작업 종료 체크포인트 (2026-08-20)

- 단일 브랜치 `feat/wordmaster-vertical-slice`와 단일 작업 경로에서만 작업했습니다. 별도 worktree·복제·중복 브랜치를 만들지 않았습니다.
- 최신 단어 빌더의 실제 수용량 실행은 기초 500·유치원 500·초등학교 1,500·중학교 2,500, 전체 canonical family head·family ID·lemma 5,000개 고유로 통과했습니다. 유치원은 안전한 A1 466개를 모두 우선 사용하고 source CEFR A2 34개만 부족분으로 보충했습니다.
- 독립 코드 리뷰에서 확인한 추적 스키마, 저장 상한, mount 저장 경합, 학습 큐 감사 시점, 문법 숙달 지표, CSS 토큰 결함 6건은 수정했고 재리뷰 결과 같은 범위의 잔여 결함은 0건입니다.
- 사용자가 네 자동 소설의 개선 정본을 승인했고 `Archimentor`를 reviewer로 기록했습니다. 각 정본은 도입·갈등·전환·결말이 이어지는 8문단 본편과 별도 전체 어휘 장면을 포함하며, 승인 시각 `2026-08-20T03:34:02.062Z`와 전체 payload SHA-256 digest를 고정합니다. 본편·어휘 장면·사용 단어 중 하나라도 바뀌면 검증이 실패합니다.
- `npm run content:build`는 승인 입력을 포함해 중단 없이 완료되어 검증된 콘텐츠 산출물 17개를 원자적으로 승격했습니다. 단어 5,000개·구동사 1,000개·문법 노드 42개·승인 소설 4개가 development와 release 검증을 통과했고 build lock·staging·rollback·swap residue는 남지 않았습니다.
- 하이픈 결합어의 일부 문자열을 독립 표제어 예문으로 오인하던 원천 수집 결함을 수정했습니다. `eight`의 `forty-eight` 예문은 독립형 원천 예문으로 교체됐고 provenance 줄 번호도 함께 재생성됐습니다.
- `npm run check`는 ESLint, 57개 테스트 파일의 780개 테스트, TypeScript·Vite 빌드, self-contained 오프라인 검증까지 모두 통과했습니다. `npm audit`은 취약점 0개이고, 단어군 996개/구성원 2,285개/근거 1,299개, Python 번역 테스트 14개와 고정 원본 9종의 SHA-256 검증도 통과했습니다.
- 루트 `index.html`과 `dist/index.html`은 최신 소스에서 다시 생성됐고 SHA-256이 같습니다. `npm run verify:offline`은 두 파일이 최신·자급식이며 DOM 렌더가 가능함을 확인했습니다.
- `npm run test:browser`의 Chromium 시나리오 12개가 데스크톱·모바일에서 모두 통과했습니다. 루트 `index.html` 직접 `file://` 실행, HTTP 핵심 여정, 키보드 전용 흐름, 320px 긴 콘텐츠·복구·폼 레이아웃을 포함합니다.
- 브라우저 플러그인으로 최신 `dist/index.html`을 로컬 HTTP에서 열어 서사형 본편의 4→8문단 확장, 기본 닫힘인 전체 어휘 장면, 본편·연습 단어 상세, 상세 닫기 뒤 트리거 포커스 복귀를 조작했습니다. 문서 `scrollWidth`와 `clientWidth`는 1,265px로 같고 콘솔 오류는 0건이었습니다. 플러그인의 보안 정책이 `file://` 탐색을 차단하므로 파일 직접 실행과 320px 모바일 증거는 위 Chromium 회귀 테스트로 확보했습니다.
- `npm run validate:release`는 단어 5,000개·구동사 1,000개·문법 노드 42개·승인 소설 4개로 성공했습니다. 작업 파일은 아직 stage·commit·push하지 않았습니다.

## 1. 구현된 핵심 수직 슬라이스

| 영역 | 현재 구현 내용 | 검증 근거 |
| --- | --- | --- |
| 기반 | React·TypeScript·Vite 프로젝트, 콘텐츠·학습자 상태 v7·추적 v2 JSON 스키마와 런타임 검증, 엔진 규칙·이전/릴리스 계약, 개발/릴리스 검증 분리, 루트 `index.html`의 오프라인 실행 | `tests/data-contract.test.ts`, `tests/runtime-contract-data.test.ts`, `npm run check`, `npm run verify:offline` |
| 상태 | 버전 7 로컬 상태와 v1~v6 이전, 구조화 문법 산출·수동 검수·교정 사이클 복원, 손상·이전 원문 복구 백업·내려받기, 손상 복구, 저장 실패 시 탭 메모리 대체, 메뉴·학습 세션·단어·문법 숙련도·최근 7회 퀴즈·레벨별 퀴즈 난이도 정답 통계 및 선택/노출 분석, 일별 활동·항목 스케줄·문항별 응답/유형 누계·세션/큐 감사 이력 복원 | `src/state/*.test.ts*` |
| 탐색 | 7개 최상위 메뉴, 문맥별 하위 메뉴, 명시적 학습·퀴즈 진입 단계, 레벨 대시보드·소설·단어장, A1~C1 문법 | `src/app/App.test.tsx` |
| 참조 | 단어·구동사 통합 검색·종류 필터와 100개 단위 점진 표시, 완료·미완료 개별 목록, 42개 문법 노드와 관련 카탈로그 어휘, 레벨별 서사형 본편·접이식 전체 어휘 장면과 커버리지 표시 | 각 feature 테스트 |
| 학습 | 최대 500개 고유 큐, bounded top-N 추출, 카드 플립, 의미·예문, 5단계 난이도, 학습·소설 TTS, 레벨별 세션, 항목 스케줄·숙련도·취약 문법을 반영한 큐와 생성/복구 감사 이력 | `StudyView.test.tsx`, `StoryView.test.tsx`, `queue.test.ts` |
| 퀴즈 | 6종 문항, 활용형 구동사 문장 판정, 부분 표본 추출 기반 적응형 출제, 즉시 채점·시도/숙련도 저장, 유형별 오답 이유·정답 해설, 수동 다음, 결과 통계, 오답 전용 복습, 문항 반응시간·재노출·실제 보정·중단/완료 세션 추적 | quiz 단위·통합 테스트 |
| 연동 | 문항별 오답 +15%/+30%, 퀴즈 중단 전 즉시 복습 예약, 서로 다른 카드 최소 간격, 연속 오답 첫 3슬롯 우선 노출, 현재 레벨의 퀴즈 난이도 정답 통계와 선택/실노출/재노출 분석, 문법 취약점→관련 어휘→학습/퀴즈 재가중, 문법 다음 학습/재진단 추천 | mastery·queue·grammar recommendation·상태·앱 통합 테스트 |
| 품질 | 로딩 제한시간·형제 요청 취소·재시도·경합 방지, 빌드 시 카탈로그 검증과 레벨별 지연 정규화, 커밋 전 복원·커밋 후 residue 검증을 포함한 콘텐츠 세대 교체, 본편의 도입·갈등·전환·결말 및 목록형 회귀 차단, 본편+어휘 장면의 품사 독립 레벨 소유권·고정 주인공 이름 외 비카탈로그 토큰 차단, 키보드 접근성, 390px 반응형 | 앱 자동 테스트와 Playwright 데스크톱·모바일·루트 `file://` 시나리오 |

현재 카탈로그는 단어 5,000개(기초 500·유치원 500·초등학교 1,500·중학교 2,500), 구동사 1,000개(레벨별 250), 문법 노드 42개, 승인 소설 4개(`isManual: true`)입니다. 수량·구조·참조·승인 provenance 검증은 이 데이터로 실행됩니다.

자동 인수 명령은 `npm run check`와 `npm run test:browser`입니다. Vitest는 `.worktrees`와 `tests/browser`를 제외한 현재 checkout만 수집하고, Playwright는 데스크톱·모바일 HTTP 흐름과 루트 `index.html`의 직접 `file://` 실행을 검사합니다. 테스트 파일·테스트 개수는 변경될 수 있으므로 문서에 고정하지 않고 각 명령의 실패 0개를 기준으로 하며, 실제 production URL은 배포 후 별도 smoke test 대상으로 둡니다.

이번 체크포인트에서는 `npm run validate:release`, `npm run check`, `npm run test:browser`와 브라우저 플러그인 조작 검증까지 통과했습니다. 정적 오프라인 검증, 실제 브라우저 게이트, 원격 production 배포는 서로 구분합니다.

상태 버전 4의 전역 퀴즈 난이도별 시도·정답, 선택 난이도·실제 노출·오답 재노출 누계는 버전 5 형식을 거쳐 당시 `navigation.level` 버킷에 보존됩니다. 버전 5는 빈 추적 상태를 추가해 버전 6으로 이전하고, 버전 1~3과 초기 메뉴 형식도 같은 경로를 거칩니다. 버전 6→7에서는 신뢰 가능한 누적 문법 `productionAttempts`·`retryCount`·`errorCounts`를 보존하되, 구형 문자열 근거를 새 구조화 근거로 추정하지 않기 위해 기존 산출 승인·active 재진단 결과·연속 오류·완료 게이트를 초기화합니다. 이후 새 제출은 보존된 누적 산출 횟수 다음 번호로 시작합니다. 복구 백업은 정상 버전 7 값을 갱신할 때마다 만드는 사본이 아니라, 앱 시작 시 손상되었거나 이전 대상인 저장 원문을 바이트 그대로 보존하는 장치입니다.

소설은 `storyText` 서사형 본편과 기본적으로 닫힌 `vocabularyPracticeText` 전체 어휘 장면을 분리합니다. 본편은 단어 수와 무관하게 최소 7문단·12문장, 다문장 문단, 첫·마지막 문단의 Mina 등 사건 구조를 검사합니다. 커버리지는 두 텍스트를 합쳐 검사하지만 레벨 경계는 각각에 동일하게 적용합니다. `usedWords`의 선언 품사에만 의존하지 않고 카탈로그의 모든 형태에서 가장 낮은 소유 레벨을 적용하며, 비카탈로그 토큰은 고정 주인공 이름 `Mina`와 그 소유격만 허용합니다. 폐쇄어와 숫자도 카탈로그 소유권이 없으면 실패합니다.

## 2. 요구사항 추적표

| 요구사항 | 자동화된 근거 | 실제 화면 확인 |
| --- | --- | --- |
| 메뉴/컨텍스트 상태 전이 | `학습을 누르면 레벨 메뉴를 열고 컨텍스트 레벨 선택 즉시 학습으로 진입한다`, `단어장은 최상위 메뉴가 아니라 레벨 컨텍스트 메뉴다` | 최상위 메뉴와 레벨 하위 메뉴 전환 |
| 새로고침 복원 | `같은 저장소로 다시 렌더링하면 메뉴 선택을 복원한다`, `일반 학습 세션은 평가 후 새로고침해도 다음 위치를 복원한다` | 메뉴·학습 카드 변경 후 새로고침 |
| 단어+구동사 검색 | `단어와 구동사를 같은 검색 결과에서 형태·뜻까지 검색한다` | 단어장에서 `wake up` 검색 |
| 단어장 종류 분리 | `일반 단어와 구동사를 종류별로 필터링한다` | 단어장의 항목 종류 필터 전환 |
| 완료·미완료 개별 관리 | `완료·미완료·오답을 현재 레벨의 단어와 구동사에서 집계한다` | 대시보드에서 두 목록 전환·점진 표시 |
| 42개 문법 노드 탐색 | `문법 대시보드는 전체 42개와 레벨별 수량을 보여준다`, `전체 문법 순서로 이전·다음 이동하고 stale 노드는 안전하게 처리한다` | A1~C1 노드와 이전·다음 이동 |
| 문법-단어장 정합성 | 문법 예문·연습·오류 교정에 실제로 등장하는 카탈로그 어휘를 형태 기준으로 연결 | 문법 노드의 관련 단어·레벨·뜻 확인 |
| 최대 500개 비복원 학습 큐 | `저장 세션이 없으면 최대 500개 고유 큐를 한 번 생성해 즉시 저장한다`, `returns exactly 500 unique items from 600 candidates` | 학습 진행률과 새로고침 후 위치 |
| 카드 의미+예문 동시 노출 | `카드를 뒤집으면 모든 뜻과 예문을 보여주고 다음 카드에서 닫힌다` | 플래시카드 뒤집기 |
| 5단계 난이도 | `uses the documented 5 by 5 matrix`, `난이도 변경은 현재 카드를 유지하고 뒤쪽만 재가중하며 숙련도를 건드리지 않는다` | 다섯 난이도 버튼 |
| TTS 실패 대체 | `필수 브라우저 API가 없으면 발음 포트를 만들지 않는다`, `TTS 실패를 알리되 카드 학습은 계속할 수 있다` | 음성 미지원 상태의 비차단 안내 |
| 6종 퀴즈 | `여섯 유형의 전체 한국어 이름을 표시하고 선택 유형을 바꾼다`, `creates a %s question with an answer and source` | 여섯 유형 선택·문항 렌더링 |
| 즉시 화면 피드백+수동 다음 | `객관식은 즉시 피드백 후에도 다음문제 전까지 같은 문항을 유지한다`, `같은 문항을 연속 제출해도 시도를 한 번만 기록한다`, 유형별 불일치 이유와 정답 근거를 표시 | 오답 제출 직후 저장·오답 이유·정답 해설·피드백과 `다음문제`, 중도 이탈 후 상태 확인 |
| +15%/+30% 오답 보정 | `applies the documented single and streak mistake boosts`, `같은 퀴즈 오답이 누적되면 다음 학습 큐 첫 3슬롯으로 전파된다`; 각 문항 제출 즉시 적용 | 퀴즈 오답 후 학습 재진입 |
| 레벨별 학습 분석 격리 | `ADVANCE_STUDY_SLOT`과 `RECORD_QUIZ_ATTEMPT`가 명시된 레벨 버킷만 갱신하고 앱과 스케줄러가 현재 레벨 분석만 사용한다 | 서로 다른 두 레벨의 퀴즈 정답률·선택·노출·재노출 수치 전환 |
| 상태 v7 상세 추적·복원 | `updates ease, due time, item schedule, last level, and daily totals from provided time`, `persists quiz responses once and accumulates average time, reexposure, wrong runs, and adjustment`, v1~v6/legacy 이전·구조화 산출 왕복 영속화 테스트 | 학습·퀴즈·문법 산출 후 새로고침하고 일별 활동·항목 일정·세션/큐·산출 근거 이력 확인 |
| 스케줄·숙련·문법 통합 큐 | `스케줄·숙련·문법 규칙을 큐 순서와 음수 보존 평균 감사값에 반영한다`, `출제에 사용한 schedule·mastery·오답·문법 보정의 실제 감사 가중치를 저장한다` | 취약 문법 학습 뒤 관련 어휘가 학습·퀴즈 후보에 반영되는지 확인 |
| 문법 추천·재진단 | `prioritizes a valid must-review requirement as focused review`, `routes an approved production into the rediagnostic queue`, locked/stale 제외 테스트 | 문법 대시보드의 추천 학습·집중 복습·재진단 큐와 선행 통과율 확인 |
| 운영 지표 대시보드 | `연속 학습·유형 반응 지표·큐 건강도를 추적 상태에서 표시한다`, 6종 유형 정확도·평균 반응·재출제 효율·연속 오답률 계산 테스트 | 레벨별 연속 학습·복습 일정·최근 세션/큐와 유형별 지표 확인 |
| 소설 형태 레벨·어휘 폐쇄성 | 등록된 모든 형태의 소유 레벨을 품사와 무관하게 검사하고 고정 주인공 이름 외 비카탈로그 토큰을 거부한다 | 상위 레벨 형태, 미등록 내용어·문법어·숫자를 넣은 데이터 검증 실패 |
| 소설 본편·어휘 장면 분리 | `storyText`는 완결된 8문단 서사로 표시하고 `vocabularyPracticeText`는 닫힌 별도 연습 영역에서 문단 단위로 점진 렌더링한다 | 본편 문단 경계·서사 구조·목록형 회귀, 연습 영역 disclosure·단어 상세·합산 100% 커버리지 테스트 |
| 결과 점수·정답률·히트맵·오답 유형 | `summarizes score, accuracy, every type, heatmap, and unique wrong items`; 결과 시점에는 세션 요약만 한 번 기록하고 개별 시도는 중복 저장하지 않음 | 퀴즈 결과 화면 |
| 개발/릴리스 데이터 검증 분리 | `development validation and CLI succeed with concise counts`, 릴리스 모드에서 목표 수량과 소설 수동 검수 플래그를 함께 검사 | 두 npm 검증 명령의 종료 코드와 출력 |

테스트 이름은 현재 코드의 실제 테스트명이며, 관련 파일은 `src/app`, `src/features`, `src/domain`, `src/state`, `tests` 아래에 있습니다.

## 3. 남은 검수 콘텐츠

| 콘텐츠 | 현재 | 릴리스 목표 | 남은 작업 |
| --- | ---: | ---: | --- |
| 단어 | 기초 500, 유치원 500, 초등학교 1,500, 중학교 2,500, 총 5,000 | 5,000 | 목표 수량은 충족. 표본 편집 QA는 지속하되 현재 콘텐츠 릴리스 검증의 차단 조건은 아님 |
| 구동사 | 레벨별 250, 총 1,000 | 1,000 | schema-v5 단일 정본의 1,000행 전수 기계 보조 감사 완료. 원천 좌표 예문 페어 617행과 정의 조건부 편집 초안 383행, 미해결 교차참조 0건을 빌드에서 강제. 사람 편집 승인은 아님 |
| 문법 노드 | 42 | 42 | 각 노드의 규칙·패턴·예문·진단/연습/재진단·수준별 산출 과제·오류 노트·숙련 기준과 UI 상호작용 구현 완료. A1~B1 문장 수, B2 네 단락·복합문 근거, C1 두 레지스터·2회 교정 한도를 데이터·런타임·저장 계약에서 강제하고 모든 의미 판정은 사용자가 선택한 문장 근거를 수동 검수함. 교육 문안의 사람 편집 QA는 지속 품질 활동이며 기술 누락이나 현재 자동 게이트 차단 조건은 아님 |
| 소설 | 레벨별 서사형 본편 1개 + 전체 어휘 장면 1세트, 총 4개 승인 정본 (`isManual: true`) | 레벨별 정확히 1개 읽기 패키지, 총 4 | reviewer·reviewedAt·본편/어휘 장면 전체 source digest를 포함한 승인 정본 완료. 이후 두 텍스트 중 하나라도 수정하면 다시 승인해야 함 |

로그인, 서버 데이터베이스, 여러 기기 동기화, 관리자 CMS는 현재 범위의 비목표입니다.

## 4. 후속 작업 순서

1. 사용자가 승인한 범위대로 현재 `feat/wordmaster-vertical-slice` 한 브랜치에서 의도된 파일만 stage·commit하고, 중복 브랜치나 worktree 없이 원격 `main`으로 직접 게시합니다.
2. `.github/workflows/pages.yml`의 전체 검증·배포 job이 성공했는지 확인합니다.
3. `https://archimentor.github.io/EnglishWords/`를 브라우저 플러그인으로 열어 제목·서사형 본편·접힌 어휘 장면·단어 상세·콘솔 오류·문서 폭을 production 카나리 검사합니다.

대량 콘텐츠의 수량 달성과 구조 검증은 모든 향후 교육적·편집적 QA가 끝났다는 뜻은 아닙니다. 다만 현재 네 소설은 단어 나열형 본문을 제거하고 서사형 본편과 별도 전체 어휘 장면으로 분리했으며, 사용자가 자동 소설 정본을 승인했습니다. 승인 digest는 본편·어휘 장면·사용 단어 전체를 묶으므로 어느 한 부분이 바뀌어도 재승인이 필요합니다.

`content:build`는 승인 정본이 있는 레벨의 사람 편집본을 그대로 검증·보존하고, 승인 파일이 없는 레벨만 `isManual: false` 자동 초안으로 생성합니다. 승인 metadata나 story digest가 틀리면 커밋 전에 실패하며, provenance는 각 레벨을 `approved-manual-input` 또는 `automated-draft`로 구분합니다. 자동 초안을 빌드가 스스로 승인 상태로 올리는 경로는 없습니다.

## 5. 품질 및 릴리스 게이트

### 앱 수직 슬라이스 게이트

```powershell
npm ci
npm run check
npm run test:browser
```

개발 데이터 검증, ESLint, 현재 checkout 단위·통합 테스트, TypeScript와 Vite 빌드, self-contained 오프라인 렌더 검증, Playwright 브라우저 시나리오가 모두 성공해야 합니다. 데스크톱과 모바일에서 대시보드 → 소설 단어 상세 → `wake up` 검색 → 카드 뒤집기·난이도 기록 → 퀴즈 오답 즉시 피드백 → 명시적 `다음문제` → 결과 → 우선 복습 → 새로고침 복원 흐름이 오류 없이 동작해야 하며, 루트 `index.html`도 네트워크 자산 없이 직접 열려야 합니다.

### 전체 콘텐츠 릴리스 게이트

```powershell
npm run validate:release
npm run check
npm run test:browser
```

세 명령이 모두 exit code 0이어야 전체 콘텐츠 릴리스 준비가 완료됩니다. 최신 승인 입력 기반 `content:build`, `validate:release`, `check`, `test:browser`가 모두 exit code 0으로 완료됐습니다. 로컬 콘텐츠 릴리스 차단 항목은 0건이며, 남은 게이트는 원격 저장소 연결과 Pages production 카나리입니다.

## 6. 변경 원칙

- `개발계획.md`, `개발(구조).md`, `개발(문법).md`를 제품 요구사항의 기준으로 유지합니다.
- 화면은 상태 전환만 요청하고, 큐·채점·숙련도 계산은 `src/domain`의 순수 함수로 둡니다.
- 브라우저에서 읽는 콘텐츠의 정규 경로는 `public/data`입니다.
- 행동을 바꿀 때는 실패하는 테스트를 먼저 추가하고 `npm run check`로 회귀를 막습니다.
- 개발 수량과 릴리스 수량을 UI·문서·검증 출력에서 항상 구분합니다.

## 7. 원문 목표와 채택 구현의 관계

초기 원문에는 대안 또는 미래 구조도 함께 기록되어 있습니다. 현재 채택 구현은 다음과 같이 확정했습니다.

- 예상 스택 중 React를 선택하고 TypeScript 6·Vite 8·plain CSS로 구현했습니다. Vue, Tailwind, Zustand, Recoil, Express는 사용하지 않습니다.
- 상태는 React Context와 reducer, 브라우저 `localStorage`로 관리하며 저장소 접근 실패 시 탭 메모리로 대체합니다.
- 최상위 메뉴는 상세 상태 전이를 정의한 `개발(구조).md`에 따라 네 레벨과 `문법/학습/퀴즈`로 구성하며, `단어장`은 레벨 컨텍스트 메뉴입니다.
- 브라우저 정적 데이터 경로는 원문의 예시 `data/...`를 Vite 관례에 맞춰 `public/data/...`로 확정했습니다.
- 문법은 노드별 HTML 파일을 생성하지 않고 `public/data/grammar/nodes.json`을 단일 데이터 소스로 렌더링합니다.

원문 문서는 제품 목표와 의도를 보존하고, 이 문서와 승인된 수직 슬라이스 설계는 현재 실제 구현 상태를 설명합니다.
