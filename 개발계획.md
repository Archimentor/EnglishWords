# 영단어 5000 암기 웹사이트 개발 계획서

## 1. 프로젝트 개요

- **프로젝트 명칭:** 영단어 5000 마스터
- **프로젝트 목표:** 기초부터 중학교 수준까지의 필수 영단어 5000개를 사용자가 쉽고 재미있게 암기할 수 있는 웹 서비스 제공
- **타겟 사용자:** 영어 학습을 시작하는 초심자 및 초/중학생

## 2. 주요 기능

### 2.0. 메뉴 구조
- **최상위 메뉴:** `기초`, `유치원`, `초등학교`, `중학교`, `단어장`, `문법`, `학습`, `퀴즈`
- **동작 규칙:**
  - `학습` 클릭 시만 레벨 하위 메뉴(`기초`, `유치원`, `초등학교`, `중학교`)를 표시한다.
  - `퀴즈` 클릭 시에도 동일하게 레벨 하위 메뉴를 표시한다.
  - 학습 레벨 선택 즉시 단어학습 실행 모드로 진입한다.
  - `퀴즈` 레벨 선택 즉시 퀴즈 유형 선택 화면으로 이동한다.
  - 레벨 전환 시점에도 학습 큐 상태(진행률, 오답 가중치)는 초기화하지 않고 다음 세션에서 유지한다.

### 2.5. 구동사 기능 (추가)
- **구동사(verb+particle) 목록:** 자주 사용하는 구동사 1000개를 별도 데이터셋으로 별도 수집
- **커버리지 규칙:** 구동사는 `기초`, `유치원`, `초등학교`, `중학교`에서 골고루 사용되어야 하며, 각 레벨별 학습 항목에서 대상 구동사 100% 커버리지를 달성해야 한다.
  - 권장 분배: 레벨당 250개(총 1000개).  
  - 편차 허용 범위가 필요한 경우 문서화된 기준으로 사전 합의한다.
- **범위:** 전체 난이도 레벨과 직접 대응하지 않는 별도 카테고리로 운영하되, 검색·학습·퀴즈에서 일반 단어처럼 조회 가능
- **데이터 항목:** `baseVerb`, `particle`, `phrasalVerb`, `levelHint`, `meaningKo`, `examples`, `partOfSpeech`, `usageNotes`

### 2.1. 단어장 기능
- **레벨 선택:** 기초(500), 유치원(500), 초등학교(1500), 중학교(2500) 레벨 선택 기능
- **단어 목록:** 선택한 레벨의 단어, 뜻, 품사, 예문을 한눈에 볼 수 있는 목록 제공
- **단어 검색:** 원하는 단어를 쉽게 찾을 수 있는 검색 기능

### 2.2. 문법 기능
- **문법 카테고리:** 시제, 조동사, 비교, 접속사, 문장 구조 등 레벨별 문법 목록 제공
- **문법 예문:** 문법별 예문을 2~3개씩 제공하고 레벨 규칙에 맞게 난이도 조절
- **문법 연습:** 문장 바꾸기, 오답 포인트 표시, 즉시 해설 제공
- **문법 정합성:** 단어장과의 정합성을 유지하고 레벨 이동 시 이전/이후 단계 문법의 범위를 구분

### 2.3. 학습 기능
- **플래시카드:** 레벨 선택 후 시작되는 단어 학습 모드로 진입한다.
- **단어 노출 수:** 매 회차 시작 시 레벨별로 무작위 500개 단어를 섞어 제시한다.
- **카드 인터랙션:** 카드 뒤집기 동작에서 기본단어와 함께 **의미 + 예문**을 모두 표시한다.
- **난이도 버튼 5단계:** `아주쉬움`, `쉬움`, `보통`, `어려움`, `아주어려움` 선택이 가능하다.
- **난이도별 노출 우선순위(기본값):**
  - 아주쉬움: 아주쉬움 45% / 쉬움 30% / 보통 20% / 어려움 4% / 아주어려움 1%
  - 쉬움: 아주쉬움 25% / 쉬움 35% / 보통 25% / 어려움 10% / 아주어려움 5%
  - 보통: 아주쉬움 10% / 쉬움 25% / 보통 40% / 어려움 15% / 아주어려움 10%
  - 어려움: 아주쉬움 5% / 쉬움 15% / 보통 30% / 어려움 30% / 아주어려움 20%
  - 아주어려움: 아주쉬움 2% / 쉬움 8% / 보통 20% / 어려움 35% / 아주어려움 35%
- **발음 듣기:** 원어민 발음을 들으며 정확한 발음을 익히는 TTS 기능
- **발음/의미/예문:** 카드 플립 시 한 화면에서 동시 확인 가능

### 2.4. 퀴즈 기능
- **총 6개 유형 필수 제공:**
  - 유형 1: `4지선다` 영어→한글(단어 보기로 뜻 선택)
  - 유형 2: `4지선다` 한글→영어(뜻 보기로 단어 선택)
  - 유형 3: `4지선다` 문장 보기 + 밑줄 처리 단어 의미 선택
  - 유형 4: `4지선다` 문장 보기 + 빈칸에 들어갈 단어 찾기
  - 유형 5: `받아쓰기` 듣기 기반 스펠링 입력(발음 듣고 입력)
  - 유형 6: `짧은 문장 변환` 단어 하나를 문장에서 써 넣기(직접 입력형)
- **상호작용 규칙(모든 유형 공통):**
  - 선택(클릭) 시 즉시 정답 공개
  - 정답 전후와 관계없이 다음 문항은 `다음문제` 버튼 클릭 시 이동
  - 오답 시 정답 보기와 오답 이유를 즉시 노출하고, 오답 단어는 우선 재출제 후보로 등록
  - 난이도 분포는 학습의 난이도 버튼 선택값과 퀴즈별 실 정답률 기반으로 계산
- **퀴즈 결과:**
  - 퀴즈 종료 후 점수, 정답률, 정답 히트맵, 틀린 단어 목록, 오답 유형 분포 제공
- **학습 연동 보정 규칙:**
  - 학습에서 선택한 난이도(예: 아주쉬움/쉬움)라도, 해당 난이도 단어군에서 정답률이 낮으면 해당 단어의 학습 가중치를 자동 상향한다.
  - 보정 규칙:
    - 틀린 횟수 1회: 다음 노출 확률 +15%
    - 연속 틀린 횟수 2회 이상: +30%, 다음 3회 학습 슬롯에서 상위 노출 우선
    - 정답률 60% 미만의 난이도 그룹: 난이도별 기본 가중치에서 다음 세션 노출 비중을 전체 +10% 반영
  - 퀴즈에서 복기한 단어는 학습 큐에서 최소 1회 더 빨리 재노출되며, 최소 간격 규칙을 만족한 뒤 재출제한다.
- **퀴즈/학습 동기화 지표:**
  - 난이도별 노출 정체성: 학습 버튼 선택 비율, 퀴즈 정답률, 최근 7회 오답 패턴을 함께 저장한다.
  - 동일 단어는 최근 오답이 해소될 때까지 난이도 버튼 선택과 무관하게 재노출 우선권을 가진다.

### 2.5. 학습 관리 기능
- **학습 현황:** 레벨별 학습 진행률(%) 시각적 표시 (예: 진행 바)
- **오답 노트:** 퀴즈에서 틀린 단어만 모아 다시 학습하고 퀴즈를 볼 수 있는 기능
- **완료 단어/미완료 단어:** 암기 완료한 단어와 아직 외우지 못한 단어를 구분하여 관리

## 3. 기술 스택 (예상)

- **프론트엔드 (Frontend):**
    - **Framework:** React.js 또는 Vue.js (컴포넌트 기반으로 인터랙티브한 UI 구현에 용이)
    - **Styling:** Tailwind CSS 또는 Bootstrap (빠르고 반응형 디자인 적용)
    - **State Management:** Zustand 또는 Recoil (상태 관리 라이브러리)

- **백엔드 (Backend):**
    - **Framework:** Node.js + Express.js (프론트엔드와 동일한 JavaScript 생태계를 사용하여 개발 효율성 증대)
    - (초기 단계에서는 별도 백엔드 없이, 프론트엔드에서 단어 데이터를 JSON 파일 형태로 관리하는 것도 가능)

- **데이터베이스 (Database):**
    - 초기에는 `JSON` 파일로 단어 데이터를 관리하여 서버 없이 간단하게 시작
    - 사용자별 학습 데이터 저장이 필요해질 경우 `SQLite` 또는 `Firebase`와 같은 경량 데이터베이스 도입 고려

## 4. 개발 단계 (로드맵)

### 1단계: 레벨별 워드리스트 파일 구축 (최우선)
- **목표:** 학습 단계별 기준 단어셋을 확정하고 파일로 분리
- **중요 규칙:**
  - 레벨은 `기초(영아기), 유치원, 초등학교, 중학교` 순서로 나눈다.
  - 각 레벨에 속한 단어만 포함하고, 상위 레벨 단어를 하위 레벨에 섞어 넣지 않는다.
  - 레벨 간 단어는 중복되지 않는다.
  - 한 워드패밀리에 속한 단어는 원형(lemma) 기준으로 1개만 둔다.
  - 워드리스트는 레벨별로 별도 파일로 분리한다. 예: `data/wordlists/beginner.json`, `data/wordlists/level-01.json`
- **추가 산출물:** 자주 사용하는 구동사 1000개는 별도 파일로 분리한다.
  - `data/phrasal-verbs/top-1000.json`
  - `data/phrasal-verbs/by-level/기초.json`
  - `data/phrasal-verbs/by-level/유치원.json`
  - `data/phrasal-verbs/by-level/초등학교.json`
  - `data/phrasal-verbs/by-level/중학교.json`
- **구동사 검수 규칙:**
  - 상기 4개 레벨 파일의 구동사 수를 합산해 1000개를 충족해야 한다.
  - 각 레벨 파일이 해당 레벨 대상 구동사를 100% 커버해야 한다.
- **출력:**
  - 레벨별 기본 단어 목록(`word`, `lemma`, `level`, `isFamilyHead`, `familyId`)

### 2단계: 다품사/동사변형 정의
- **목표:** 단어 항목에 `원어-형태`를 정리해 검색/표기 일관성을 확보
- **작업:**
  - 각 단어에 대해 해당 레벨에서 쓰이는 품사만 기재한다.
  - 동사인 경우 가능한 활용형(현재/과거/과거분사/현재분사/3인칭단수 등)을 정리한다.
  - 품사와 동사변형은 레벨별 워드리스트 파일에서 즉시 검증 가능해야 한다.
- **체크포인트:**
  - 원어가 동일하더라도 품사별 항목은 구분한다.

### 3단계: 한국어 의미(다의어 정리)
- **목표:** 다품사/동사변형별 한국어 의미를 부여한다.
- **작업:**
  - `원어 + 다품사 + 동사변형` 조합마다 뜻을 매핑한다.
  - 한 단어가 여러 뜻을 가질 경우 사용 빈도 높은 의미 3~4개만 선별한다.
- **품질 조건:**
  - 의미는 쉬운 난이도에서 어려운 난이도로 나열하지 않고, 해당 연령대 사용 빈도 기준으로 정렬한다.

### 4단계: 발음기호 부여
- **목표:** 단어-품사 조합별로 음성 표기 체계를 정비한다.
- **작업:**
  - IPA 표기(또는 통일된 발음 표기 규칙)로 기재한다.
  - 동사변형 항목(복수형/과거형 등)에 대한 발음도 함께 제공할 수 있으면 함께 기록한다.
- **구동사 규칙(추가):**
  - 기본 동사와 분리된 형태가 아닌 **구동사 1000개는 별도 데이터셋**으로 관리한다.
  - 구동사 항목은 `phrasalVerb` 단위로 등록하고, 필요 시 기본 동사와 동시 연결할 수 있도록 한다.
  - 구동사 데이터셋은 레벨별 커버리지를 100%로 채우고, 기초→중학교로 균형 있게 분배한다.
- **출력 파일 예시:** `data/phrasal-verbs/top-1000.json`

### 5단계: 예문 데이터 작성
- **목표:** 실제 사용 맥락을 통해 단어 암기 정확도를 올린다.
- **작업:**
  - `원어 + 다품사 + 동사변형`별로 예문을 2개씩 작성한다.
  - 같은 예문이 반복되지 않게 하고, 난이도에 맞게 간결한 문장으로 구성한다.
- **체크포인트:**
  - 각 예문이 해당 의미를 자연스럽게 반영해야 한다.

### 6단계: 레벨별 소설 작성
- **목표:** 레벨별 소설 1개를 수동으로 완성해 어휘 사용 맥락을 강화한다.
- **필수 조건 (반드시 충족):**
  - 자동 스크립트/자동 생성은 사용하지 않는다(수동 작성).
  - 각 레벨 소설에 `해당 레벨 단어 커버리지 100%`를 맞춘다.
  - 하위 레벨은 상위 레벨 단어를 사용하지 않는다.
  - 상위 레벨은 하위 레벨 단어를 사용할 수 있다.
  - 기초(영아기) 소설은 기초 단어군(워드패밀리 포함)만 사용한다.
- **검수 규칙:**
  - 소설의 사용 단어가 레벨 규칙을 만족하는지 QA 체크.
  - 단어 누락과 불필요한 상위단어 사용이 없는지 확인.

### 7단계: 학습 기능 구현
- **목표:** 완성된 데이터를 UI 기능과 연결한다.
- **세부 내용:**
  - **학습 진입 플로우:** 상단 메뉴 `학습` 클릭 → 레벨 선택 → 단어학습 시작
  - **UI 설계:** 메인 화면, 레벨 선택 화면, 단어장 화면 와이어프레임 설계
  - **단어 500개 셔플링 구현:** 레벨별 단어군에서 매 세션 500개를 추출해 랜덤 정렬 후 제시
  - **플래시카드 구현:** `단어-의미-예문` 동시 노출, 클릭/터치로 전체 뒤집기
  - **난이도 선택 UX:** `아주쉬움/쉬움/보통/어려움/아주어려움` 버튼을 세션 시작 및 중간 전환 가능하게 구현
  - **퀴즈 진입 플로우:** 상단 메뉴 `퀴즈` 클릭 → 레벨 선택 → 6개 유형 중 1개 선택 → 문제풀이
  - **퀴즈 기본 동선:** 모든 유형에서 정답 클릭 즉시 노출 후 `다음문제` 버튼으로만 진행
  - **퀴즈 연동 보정 엔진:** 난이도 버튼별 노출 가중치 + 퀴즈 오답률 기반 가중치 재계산
  - **분석 로깅:** 학습 버튼 선택 난이도, 실 노출 난이도, 퀴즈 정답률, 오답 재노출 기록 저장

### 8단계: 고도화 및 배포
- **목표:** 사용자 경험 개선 및 정식 배포
- **세부 내용:**
  - **UI/UX 개선:** 디자인 개선 및 애니메이션 추가
  - **학습 분석:** 학습 진도/오답 패턴 리포트
  - **성능 최적화:** 웹사이트 로딩 속도 및 반응성 개선
  - **배포:** Netlify, Vercel 등 정적 호스팅 서비스를 이용해 배포

## 5. 데이터 구조 (예시)

`data/wordlists/beginner.json`
```json
[
  {
    "id": "w_0001",
    "group": "basic",
    "level": "기초",
    "lemma": "play",
    "isFamilyHead": true,
    "familyId": "play-family",
    "difficulty": "veryEasy",
    "entries": [
      {
        "partOfSpeech": "noun",
        "forms": ["play"],
        "meanings": ["놀다", "연극", "장난"],
        "ipa": "/pleɪ/",
        "examples": [
          "The child likes to play in the park.",
          "We watched a great play last night."
        ]
      },
      {
        "partOfSpeech": "verb",
        "forms": {
          "base": "play",
          "s3": "plays",
          "past": "played",
          "participle": "playing",
          "pastParticiple": "played"
        },
        "meanings": ["놀다", "연주하다", "경기를 하다"],
        "ipa": "/pleɪ/",
        "examples": [
          "The kids play every afternoon.",
          "She plays the piano beautifully."
        ]
      }
    ]
  }
]
```

`data/stories/beginner.json`
```json
{
  "level": "기초",
  "title": "Little Bird and the Red Ball",
  "isManual": true,
  "coverage": {
    "mustCoverAll": true,
    "allowUpperLevelWords": false,
    "notes": "기초(영아기) 워드패밀리 단어만 사용"
  },
  "usedWords": [
    {
      "lemma": "bird",
      "partOfSpeech": "noun",
      "forms": ["bird"]
    },
    {
      "lemma": "ball",
      "partOfSpeech": "noun",
      "forms": ["ball"]
    }
  ],
  "storyText": "The baby bird is on a red ball. It wants to play. The ball is small, and the baby bird is happy."
}
```
